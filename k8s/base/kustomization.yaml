apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace-agnostic app manifests. Overlays set the namespace, the real image,
# and per-environment sizing.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
